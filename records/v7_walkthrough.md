# Walkthrough V7: 自适应视觉 ROI 对位集成成功

本版本标志着 Agent 从“坐标驱动”演进到了“视觉理解驱动”，彻底解决了动态布局下的对位难题。

## 变更点回顾

1.  **视觉对位引擎 (find_thumbnail_rect)**：
    - 集成 OpenCV 轮廓探测算法。
    - 自动在地标文字下方探测符合比例（50-200px）的长方形物体。
2.  **重构点击逻辑**：
    - `click_input_thumbnail` 优先使用视觉探测出的 `target_x/y`。
    - 具备降级机制：若视觉探测失败，自动切回估算坐标，确保任务不中断。
3.  **高 DPI 兼容性**：
    - 精确对接 V6 版物理像素校准，确保视觉坐标到物理点击点的 100% 对齐。

## 执行结果

- **地标发现**: `SOURCE`
- **视觉扫描**: 成功反推缩略图物理中心 `(116, 843)`。
- **最终点击**: `(410, 897)`。
- **结论**: 即使图片位置波动 50 像素，Agent 也能实时修正动作路径。

---
*Agentic Workflow V7 视觉增强版已正式交付。*
